import { Explorer } from "../Explorer";

export const metadata = {
  title: "Evidence · Blorenge landscape explorer"
};

export default function EvidencePage() {
  return <Explorer initialView="evidence" />;
}
